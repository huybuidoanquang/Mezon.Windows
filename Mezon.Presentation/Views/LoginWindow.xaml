<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Mezon.Presentation.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mezon.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Mezon.Presentation.ViewModels"
    mc:Ignorable="d"
    Title="LoginWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="{ThemeResource AppBackgroundBrush}">
        <StackPanel>
            <TextBlock Text="Đăng nhập" Style="{StaticResource HeaderTextStyle}"/>

            <TextBlock Text="Vui lòng nhập thông tin" 
                   Foreground="{ThemeResource AppTextSecondaryBrush}"/>

            <Button Content="Login" Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
